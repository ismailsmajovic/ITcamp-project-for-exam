const hamburger=QuerySelector("hamburger");
const meni=QuerySelector("meni");


hamburger.addEventListener("click",() => {
    hambburger.classList.toggle("active");
    meni.classList.toggle("active");    
})                  
                                            /*listner ili liener*/
document.querySelectorAll("link").forEach(n =>n.
    addEventListener("click",()=>{
        hamburger.classList.remove("active");
        meni.classList.remove("active");
    }))

    function greska(){
        var brisi=document.querySelector("body");
        brisi.style.display="none";
        
    }
    
    do{
            
        if(querySelector("logo")){

            style.width="149px";
        }
        else{

            document.write("<h1>ok<h1>");
        }
       
                            /*document.write(p + "<br>");
                            p++;*/
    
    
    }while(greska());
